<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="App3.View.HomeMenu"
             Title="Master">
    <StackLayout>
        <StackLayout>
            <Label Text="Bienvenido"></Label>
        </StackLayout>
        <StackLayout>
            <Button Text="Apuestas" Command="{Binding LoginAbreApuestaCommand}"/>

            <Button Text="Resultados" Command="{Binding LoginAbreResultadoCommand}"/>

            <Button Text="Puntaje Global" Command="{Binding LoginAbreGlobalCommand}"/>

            <Button Text="Opciones" Command="{Binding LoginAbreOpcionesCommand}"/>
        </StackLayout>
    </StackLayout>
    
      
   
</ContentPage>